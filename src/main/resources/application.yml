spring:
  application:
    name: factory-coupon-service
  config:
    import: optional:configserver:http://localhost:8888
server:
  port: 10001

eureka:
  instance:
    instance-id: factory-coupon-service
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: "*"
